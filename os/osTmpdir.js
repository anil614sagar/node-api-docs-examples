var os = require('os');

/* os.tmpdir() : Returns the operating system's default directory for temp files. */

console.log("My Operating System Temporary Directory Path : ");
console.log(os.tmpdir());
